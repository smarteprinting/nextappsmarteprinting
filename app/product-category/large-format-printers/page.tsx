import { Metadata } from 'next';
import Component from '@/components/ui/productsCategories/largeFormat/LargeFormat';

export const metadata: Metadata = {
  title: 'Professional Large Format Printers | Wide Range Printers',
  description: 'Discover professional large format printers perfect for posters, technical blueprints, architectural drawings, and oversized high-resolution banners.',
};

export default function Page() {
  return <Component />;
}
