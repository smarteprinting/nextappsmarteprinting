import { Metadata } from 'next';
import Component from '@/components/ui/blogs/posts/PrinterMaintenanceGuide';

export const metadata: Metadata = {
  title: 'Ultimate Printer Maintenance Guide: Keep it Running Smoothly | Wide Range Printers',
  description: 'Prevent printhead clogs, clear paper jams, and extend your printer lifespan with our expert ultimate printer maintenance and cleaning guide.',
};

export default function Page() {
  return <Component />;
}
